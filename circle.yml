machine:
  node:

deployment:
  release:
    tag: /v.*/
    owner: Financial-Times
    commands:
      - npm-prepublish --verbose
      - npm publish --access public

test:
  override:
    - touch no-tests
