machine:
  node:

deployment:
  release:
    tag: /v.*/
    owner: Financial-Times
    commands:
      - npm-prepublish --verbose
      - printf "_auth = $NPM_AUTH_TOKEN\nemail = $NPM_EMAIL\n" > .npmrc
      - npm publish --access public

test:
  override:
    - touch no-tests
 