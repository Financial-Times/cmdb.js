machine:
  node:

deployment:
  release:
    tag: /v.*/
    owner: Financial-Times
    commands:
      - npm whoami
      - npm login @de.tooling
      - npm-prepublish --verbose
      - npm publish --access public

test:
  override:
    - touch no-tests
